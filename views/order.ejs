<%- include('User_header') %>
    <section class="login_part padding_top">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6 col-md-6">
                    <div class="login_part_form">
                        <div class="login_part_form_iner">
                            <h3>Your Details</h3>
                            <h1><%= data.client_id.name %></h1>
                            <h4><%= data.phone %></h4>
                            <h5><%= record.number %> ,<span><%= record.street %></span> ,<span><%= record.area %></span> ,<span><%= record.city %></span> ,<span><%= record.state %></span> ,<span><%= record.country %></span>.</h5>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <%- include('User_footer') %>
    